{
  "name": "ys_admin_theme",
  "version": "0.0.0-development",
  "description": "Admin theme for YaleSites projects.",
  "scripts": {
    "develop": "sass --watch scss:css",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@namallium/tokens": "^1.35.2",
    "sass": "^1.51.0"
  }
}
